#ifndef FUNCS
#define FUNCS

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

enum{
    LPAR,
    RPAR,
    NUM,
    PLUS,
    MINUS,
    TIMES,
    DASH,
    END
};

extern int value;

#endif