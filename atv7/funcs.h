#ifndef FUNCS
#define FUNCS

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

enum tokens{
    LPAR,
    RPAR,
    NUM,
    PLUS,
    MINUS,
    TIMES,
    DASH
};

FILE* sourceFile;
#endif